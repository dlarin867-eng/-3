# Бизнес-логика — блок 6 задачника

Пока пусто. Сюда пойдёт:

- Расчёт стартовой нормы КБЖУ (формула Миффлина-Сан Жеора) — `src/config/env.ts` и модель `User` в схеме уже содержат все нужные поля (sex, age, heightCm, bodyweightKg, activityFactor, goal).
- Циклирование углеводов по тренировочным/нетренировочным дням.
- Еженедельная автокоррекция нормы по факту динамики веса (данные уже пишутся в `weight_logs` через `POST /weight`).
- Ограничение крайних значений белка выше 3,0 г/кг — серверная проверка уже есть в `api/routes/users.ts` (`PATCH /me/protein-target`), сама логика автокоррекции должна уважать ту же границу.
