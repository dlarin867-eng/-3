# Журнал сессий

*Хронологический дневник работы над проектом. Новая запись добавляется сверху, под этим заголовком. Задачи по фичам — в [[задачник]], тут — что происходило и почему.*

**Шаблон записи:**
```
## ГГГГ-ММ-ДД
**Сделано:** ...
**Дальше:** ...
**Заметки:** ...
```

---

## 2026-08-22
**Сделано:**
- Разобрали список подключённых Claude Code Skills — 1 глобальный (`find-skills`, `~/.claude/skills`) и 21 локальный, установленный именно в этот проект (`.claude/skills`, зафиксированы в `skills-lock.json`) — сводка уже была в [[карта скилов]].
- Проверили агентов на предмет «deep research»: отдельного режима/модели ни у кого не назначено, реальный веб-поиск (WebSearch/WebFetch) есть только у market-analyst, growth-marketer и частично у brand-designer.
- По скриншоту графа Obsidian нашли путаницу: черновики старого прогона «агент 1–5 — ...» в корне визуально путались с настоящими 11 агентами команды.
- Навели порядок: создали `архив\` и перенесли туда через `git mv` (история сохранена) — 5 черновиков «агент 1–5 — ...» (устарели, заменены финальными [[идея]]/[[требования к дизайну]], но остаются как источник — ссылки на них из ui-ux-designer/brand-designer/market-analyst/задачник не сломаны, Obsidian резолвит `[[wikilink]]` по имени файла) и 4 файла-мусора без отношения к проекту (`Claude` пустой, `date_of_birth.txt`, `test.txt`, `исследование.html` — старый HTML-прототип, не связан с исследование.md).
- Настраивали фильтр Graph view (`.obsidian/graph.json`) на показ только `команда` + `карта скилов` + `агенты\` — пользователь потом сам очистил фильтр вручную.
- Закоммитили перемещение в архив и эту запись журнала.
**Дальше:** По задачнику — всё ещё актуален пункт: проверка [[требования к дизайну]] дизайнером перед переносом в Figma.
**Заметки:** Договорились: журнал ≠ git-коммит — разные действия, коммичу только по прямой просьбе.

---

## 2026-08-21
**Сделано:**
- Заведён этот журнал и [[CLAUDE]] — чтобы сессии не начинались с чистого листа. Claude теперь читает журнал.md и [[задачник]] автоматически в начале сессии.
- Найден и запущен Obsidian на компьютере, открыт как vault на папке `УРОК 1`.
- Подключён MCP-сервер `obsidian` (`@modelcontextprotocol/server-filesystem`, scope local) — статус Connected; инструменты появятся с новой сессии.
- Разобрались, как передавать скриншоты в чат на Windows (Ctrl+V не работает в терминале): `Win+Shift+S` → «готово» → достаю картинку из буфера через PowerShell (`System.Windows.Forms.Clipboard`) и читаю.
- Продиагностировали, почему граф в Obsidian не похож на граф из обучающего видео — рабочие документы (идея, техзадание, задачник и т.д.) были упомянуты простым текстом в обратных кавычках, а не `[[wikilink]]`, поэтому висели в графе изолированными точками.
- Связали все такие упоминания в `[[wikilinks]]` по 22 файлам (документы + все 11 агентов) — граф теперь один связный кластер.
- Побочно найдено и исправлено: файлы в `.claude\agents\` были hard-link копиями файлов из `агенты\`, но правки через Edit-инструмент рвут hard link — пересобрали связи заново. **Важно:** это будет рваться при каждой следующей правке агента, чинить придётся вручную (PowerShell `Remove-Item` + `New-Item -ItemType HardLink`).
- В `команда.md` добавили две новые колонки в таблицы состава команды: «Кратко» (2 слова на агента, например `ui-ux-designer` → «рисует макет») и «Кто это (простыми словами)» — понятное объяснение роли для новичка.
**Дальше:** По задачнику (9/75, 12%) — ближайший незакрытый пункт: проверка текстового ТЗ ([[требования к дизайну]]) дизайнером (`ui-ux-designer`) перед переносом в Figma. После этого — блок 3 (дизайн в Figma). Отдельно: проверить MCP-инструменты obsidian после следующего перезапуска сессии (отложили в этой сессии).
**Заметки:** Пользователь пишет записи сам в конце сессии (или диктует — я оформляю и дописываю). Без хуков/автоматики, по договорённости. Решили не переименовывать файлы агентов ради подписи прямо на графе (риск порвать ссылки) — короткая метка осталась только в таблице `команда.md`.
