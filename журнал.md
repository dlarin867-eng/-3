# Журнал сессий

*Хронологический дневник работы над проектом. Новая запись добавляется сверху, под этим заголовком. Задачи по фичам — в [[задачник]], тут — что происходило и почему.*

**Шаблон записи:**
```
## ГГГГ-ММ-ДД
**Сделано:** ...
**Дальше:** ...
**Заметки:** ...
```

## 2026-08-22 (14)
**Сделано:**
- Довели живой деплой (Railway + Vercel) до полностью рабочего состояния — фаундер подтвердил: онбординг реально считает норму, аккаунт сохраняется. Два реальных бага по пути:
  1. Демо-режим — добавлена кнопка «Посмотреть демо без регистрации» (генерирует email/пароль сама, тот же настоящий `/auth/register`) — фаундер не хотел придумывать логин/пароль просто чтобы посмотреть демо.
  2. Настоящий баг валидации: шаг «Возраст, рост, вес» пропускал дальше по любому ненулевому числу (без проверки реалистичности), backend справедливо отклонял нереалистичные значения (типа роста «1») с 400 — ошибка проявлялась только в самом конце квиза, без понятной причины. Диагностировали через логи Railway (`railway logs`) — увидели повторяющиеся 400 на `/auth/register`, добавили показ конкретных полей ошибки на фронте, попросили скриншот — стало видно `heightCm`/`bodyweightKg` ниже минимума. Поправили: шаг теперь проверяет реальные диапазоны, плюс placeholder-примеры в полях.
- Итог: https://kbzhu-frontend.vercel.app полностью работает end-to-end на реальном backend+БД (Railway).
**Дальше:** Остальной блок 10 (CI/CD, мониторинг, бэкапы БД, security) и блок 11 — впереди, вероятно уже на Timeweb-сервере фаундера, а не на Railway/Vercel (см. [[хостинг]]).
**Заметки:** Хороший пример рабочего цикла для живых багов: смотреть `railway logs` → добавить подробный вывод ошибки на фронт вместо общей фразы → попросить скриншот → чинить по факту, не гадать вслепую.

---

## 2026-08-22 (13)
**Сделано:**
- Пользователь захотел не просто читать отчёты, а реально потыкать приложение — «зайти по адресу, покидать фото, потестировать». Договорились не заливать локально, а задеплоить как в обучающем видео, которое он смотрел (GymFuel AI на vercel.app) — реальные Vercel + Railway.
- Поставили PostgreSQL локально (winget) — пригодилось, т.к. до облачного Postgres напрямую с этой машины подключиться не удалось (см. ниже), пришлось генерировать первую Prisma-миграцию через локальную БД.
- Backend + Postgres задеплоены на Railway (проект, сервис БД, переменные окружения, код, публичный домен). Frontend задеплоен на Vercel с `NEXT_PUBLIC_API_URL` на backend. Оба реально работают: https://kbzhu-frontend.vercel.app и https://kbzhu-api-production.up.railway.app — подтверждено и мной (curl до Vercel), и пользователем напрямую из браузера (Railway `/health` → ok).
- По ходу нашли реальный баг: `uploadMealPhoto` падал бы с ошибкой без настроенного S3 — поправили на graceful skip (фото просто не сохраняется, если хранилище не настроено, остальной флоу не ломается).
- Важное сетевое наблюдение — зафиксировано в новом файле [[хостинг]]: из рабочей среды этой сессии не достучаться до `*.up.railway.app`/`*.proxy.rlwy.net` (обычные сайты и даже railway.com при этом отвечают нормально) — похоже на ограничение конкретно этой рабочей среды, не проблему деплоя. Подтвердили это, попросив пользователя проверить напрямую из его браузера — сработало.
- Отдельный содержательный разговор о том, что Vercel/Railway — это платные посредники поверх обычных серверов, и что можно арендовать голый VPS самому (дешевле, но вся эксплуатация на тебе). **Важно:** у пользователя уже есть оплаченный Timeweb (и подобные) — решили, что для реального релиза (блок 10) разворачиваем именно там, а Railway/Vercel — только для быстрого теста сейчас. Зафиксировано в [[хостинг]].
- [[задачник]]: блок 10 → 1/5 (настройка хостинга реально сделана и проверена), общий прогресс 46/75 (61%).
**Дальше:** Пользователь тестирует живое приложение по ссылке на frontend. Остальной блок 10 (CI/CD, мониторинг, бэкапы, security) и блок 11 (более широкое тестирование) — впереди, скорее всего уже на реальном Timeweb-сервере, а не на Railway/Vercel.
**Заметки:** Аккаунты Vercel и Railway у пользователя теперь есть и CLI на этой машине залогинены — переиспользовать при необходимости, не логиниться заново. Ключей ANTHROPIC_API_KEY/USDA_FDC_API_KEY/S3 всё ещё нет — распознавание фото по-настоящему не тестировалось, только структура/fallback.

---

## 2026-08-22 (12)
**Сделано:**
- Блок 9 закрыт (5/5) — лендинг перенесён в код на маршруте `/` того же Next.js-проекта, на общих токенах продукта (не отдельная копия дизайн-системы): hero с двумя мокапами, сравнение с обычными счётчиками, 3 фичи, демо-лента, блок доверия (ISSN, без придуманных цифр отзывов), pricing на 2 тарифа, FAQ на `<details>`, футер с дисклеймером.
- Важное уточнение спецификации, доведённое до конца: CTA лендинга ведут в `/onboarding?entry=landing`, и шаг 0 (приветствие) квиза при этом теперь реально пропускается — ровно как того требует §5.1. В блоке 8 этого разделения ещё не было (онбординг всегда показывал приветствие).
- Реально проверено: `npm run build` (11 маршрутов, 0 ошибок), `eslint .` чисто (ещё одно найденное и исправленное нарушение `react-hooks/set-state-in-effect`), curl-смок-тест подтвердил и мета-теги, и корректный пропуск шага 0.
- **Backend + Frontend по MVP (блоки 4-9) теперь полностью закрыты.** Общий прогресс задачника: 45/75 (60%).
**Дальше:** Блок 10 (DevOps — хостинг, CI/CD, мониторинг) и блок 11 (тестирование сверх уже написанных 29 юнит-тестов — кроссбраузерное, нагрузочное). Оба реальнее требуют решений фаундера (провайдер хостинга, домен), чем предыдущие блоки — не всё можно закрыть в одиночку без него.
**Заметки:** Хорошая точка, чтобы фаундер посмотрел лендинг и весь продукт вживую (нужен реальный Postgres + npm run dev в обеих папках — в песочнице разработки живой БД не было, весь backend проверялся только юнит-тестами и структурной сборкой).

---

## 2026-08-22 (11)
**Сделано:**
- Блок 8 закрыт (5/5) — все 9 экранов продукта реально подключены к backend API, не заглушки: онбординг (реальная регистрация + расчёт нормы), главный (тоггл типа дня, «Ближайшее»), фото (загрузка + распознавание с честным fallback), штрихкод (ручной ввод — сканер камерой не входил в блок), спортпит (категории → порции — справочник ≥50 позиций отдельная задача), дневник (автогруппировка, удаление), вес (график + карточка автокоррекции с рабочими Принять/Отклонить), отчёты (недельный график), настройки (профиль/дни/белок с подтверждением/тема/удаление аккаунта).
- По ходу нашёл реальный пробел в backend и закрыл его: автокоррекция веса раньше только считалась (`GET /weight/suggestion`), но не было способа её применить — добавил `POST /targets/today/apply-adjustment`. Плюс `GET /targets/history` для недельного графика отчётов.
- Столкнулись с двумя новыми строгими правилами эту версию React/eslint-config-next: `react-hooks/purity` (нельзя читать `Date.now()` прямо в теле рендера — вынес в эффект) и `react-hooks/set-state-in-effect` (нельзя синхронно звать функцию, которая вызывает setState, прямо в useEffect — обернул через `.then()`). Оба — не подавил, а исправил по-настоящему.
- Общий прогресс задачника: 40/75 (53%) — перевалили за половину. Отчёт — в [[отчёты]].
**Дальше:** Блок 9 (лендинг в коде) — дизайн и тексты уже готовы артефактом, логичное продолжение frontend.
**Заметки:** Сознательно упрощённые места, отмеченные в коде как открытые задачи: реальный сканер штрихкода камерой (нужна отдельная библиотека) и полноценный справочник спортпита с поиском на ≥50 позиций (сейчас статичный список 6 категорий). Не блокирует MVP, но не забыть при возврате к этим экранам.

---

## 2026-08-22 (10)
**Сделано:**
- Блок 7 закрыт (5/5) — Next.js (App Router, TypeScript) инициализирован в `frontend/`, дизайн-токены перенесены из требования к дизайну.md §2 напрямую в globals.css (без Tailwind — токены уже полностью специфицированы), компонентная библиотека (Button/Card/MacroRing/DayPill/ConfidenceBadge/TabBar/ScreenHeader), роутинг всех 9 экранов + онбординга, `/main` собран по-настоящему как демонстрация, остальные 8 — честные заглушки под блок 8.
- Реально проверено: `npm run build` 0 ошибок, `eslint .` чисто, dev-сервер поднят и curl'ом подтверждено рендеринг реального контента.
- Общий прогресс задачника: 35/75 (47%). Отчёт — в [[отчёты]].
**Дальше:** Блок 8 (наполнить экраны реальным содержанием + подключить к backend API) или блок 9 (лендинг в коде — дизайн уже готов артефактом).
**Заметки:** create-next-app сам сгенерировал `frontend/CLAUDE.md` + `AGENTS.md` со служебными правилами этой версии Next.js (16.3.2 — заметно новее того, что я знаю по обучению) — оставили как есть, это релевантно только для работы внутри `frontend/` и не конфликтует с корневым CLAUDE.md.

---

## 2026-08-22 (9)
**Сделано:**
- Пользователь сказал «делай всё» — продолжили backend без остановки: блок 5 (интеграции) и блок 6 (бизнес-логика) закрыты оба, 5/5 каждый.
- Блок 5: Claude API (распознавание блюда, structured tool-use), Open Food Facts (штрихкоды — сеть реально проверена), USDA FoodData Central (сверка калорийности), общий http-клиент с таймаутом/fallback, TTL-кэш нутриентов.
- Блок 6: расчёт нормы (Миффлин-Сан Жеора), циклирование углеводов по типу дня, автокоррекция по тренду веса, тайминг напоминаний + вечерняя сводка белка, жёсткий потолок белка как второй уровень защиты. Подключено к реальным роутам (`/meals/photo`, новый `/meals/barcode`, новый `targets.ts` — норма на сегодня/тоггл дня/ближайшее/вечерняя сводка, `/weight/suggestion`).
- Найден и исправлен баг типизации по ходу: `sex` в схеме БД был просто `String`, не Prisma-enum — сделали `enum Sex { m f }`, как у Goal/Theme.
- Реально проверено: `tsc --noEmit` 0 ошибок, 29 юнит-тестов бизнес-логики зелёные, сервер поднят и curl'ом проверены новые роуты. Open Food Facts — живой сетевой запрос прошёл. Claude API и USDA — код готов, но не протестирован вживую (нет реальных ключей).
- Общий прогресс задачника: 30/75 (40%). Отчёт — в [[отчёты]].
**Дальше:** Блок 7 (frontend-developer, каркас Next.js) — дизайн-система и API уже готовы, ничего не блокирует старт.
**Заметки:** Для полной проверки блока 5 понадобятся от фаундера реальные `ANTHROPIC_API_KEY` и `USDA_FDC_API_KEY` — не блокирует прогресс, но без них живой вызов не проверялся, только типизация и структура кода.

---

## 2026-08-22 (8)
**Сделано:**
- Первый реальный код проекта (не документация, не Obsidian-артефакт) — backend-каркас в `backend/`, блок 4 задачника закрыт полностью (5/5): Node.js/TypeScript/Fastify, схема БД на 6 таблиц (Prisma), валидация окружения при старте, аутентификация (регистрация/логин/JWT) с полями онбординг-квиза, роуты `/me`/`/meals`/`/weight`/`/supplements`, S3-совместимое хранилище фото с политикой короткого срока хранения.
- Не просто написано — реально проверено: `npm install`, `prisma generate`, `tsc --noEmit` (0 ошибок), поднят dev-сервер, curl'ом смок-тест здоровья/валидации/авторизации. Тестовый `.env` не оставлен в репо (в `.gitignore`).
- Общий прогресс задачника: 20/75 (27%). Отчёт — в [[отчёты]] от backend-developer.
- По ходу пользователь напомнил, что долго не было видно отчётов/прогресса, пока шла backend-работа — учтено на будущее: чаще давать промежуточные чекпоинты на многошаговых технических задачах, не только в конце большого куска.
**Дальше:** Блок 5 (интеграции: Claude API/Open Food Facts/USDA) и блок 6 (бизнес-логика: расчёт нормы, циклирование, автокоррекция) — продолжение backend; либо блок 7 (frontend-developer, каркас Next.js) параллельно, раз дизайн-система готова.
**Заметки:** Для блока 5 понадобится от фаундера реальный ключ Claude API и решение по S3-провайдеру (AWS vs Cloudflare R2) — не блокирует, но стоит решить заранее.

---

## 2026-08-22 (7)
**Сделано:**
- Блок 3 задачника закрыт полностью (5/5) — последний пункт, макеты лендинга, сделан отдельным артефактом: hero с двумя мокапами (тренировка/отдых), сравнение с обычными счётчиками, 3 фичи в фиксированном порядке (циклирование углеводов → спортпит → автокоррекция), демо-лента, блок доверия (ISSN, прозрачная формула — без придуманных отзывов/цифр), pricing на 2 тарифа с переключателем помесячно/ежегодно, финальный CTA, FAQ на 8 вопросов, футер с дисклеймером — всё по §4 требования к дизайну.md, та же дизайн-система, что и в приложении.
- Параллельно, по ходу того как пользователь тестировал прототип приложения вживую, поправили: рабочую кнопку «Назад» на шагах онбординга; кастомный стимпанк-пикер времени тренировки (точечный акцент — единственное место, где сознательно отошли от общей дизайн-системы, по прямой просьбе); скролл-колёсики для возраста/роста/веса вместо текстовых полей; профиль-карточку в «Отчётах» с кнопкой «Изменить данные» (возврат в квиз без сброса уже введённых ответов); убрали нерабочую и перекрывающую текст кнопку быстрого ввода веса, заменили на конкретную строку-предложение «2400 → 2250 ккал (−150)» с реально работающими «Принять»/«Отклонить».
- Общий прогресс задачника: 15/75 (20%). Отчёт — в [[отчёты]].
**Дальше:** Блок 3 полностью закрыт. Следующий шаг — backend (блок 4, backend-developer: инфраструктура и БД) и frontend-каркас (блок 7, frontend-developer: Next.js) можно вести параллельно, дизайн-система для обоих готова в двух опубликованных артефактах (прототип приложения + лендинг).
**Заметки:** Рабочий режим с пользователем устоялся: он тестирует опубликованный артефакт вживую, присылает скриншот + что не так, я чиню и переопубликовываю на тот же URL — без отдельного journal/commit на каждую мелкую правку (только когда закрывается пункт задачника или меняется архитектурное решение).

---

## 2026-08-22 (6)
**Сделано:**
- ui-ux-designer закрыл 3 из 4 оставшихся пунктов блока 3 одним интерактивным артефактом (не статичными картинками): полный кликабельный прототип — онбординг-квиз (9 шагов + опциональные) с реальным расчётом нормы по формуле Миффлина-Сан Жеора, и все 9 экранов продукта с рабочей навигацией между ними (главный/фото/штрихкод/спортпит/дневник/вес/отчёты/настройки), тёмная и светлая тема.
- Блок 3 → 4/5 (был 1/5). Общий прогресс задачника: 14/75 (19%).
- Отчёт записан в [[отчёты]] от лица ui-ux-designer.
**Дальше:** Последний пункт блока 3 — макеты лендинга (отдельный артефакт, другая аудитория и цель страницы). После блока 3 — backend (блок 4, backend-developer) и frontend-каркас (блок 7, frontend-developer) можно вести параллельно: дизайн-система для обоих уже готова и живёт в двух опубликованных артефактах.
**Заметки:** Прототип — хороший момент для фаундера проверить именно поведение экранов (не только картинку) до переноса в реальный Next.js-код в блоке 7 — дешевле поправить сейчас.

---

## 2026-08-22 (5)
**Сделано:**
- Начали реальную разработку продукта (не только документацию) — пользователь попросил идти от исследования к задачнику по-настоящему, с отчётами по ходу.
- ui-ux-designer закрыл последний пункт блока 2: сверил [[требования к дизайну]] с [[техзадание]] (таблица трассируемости, раздел 1) и разделом самопроверки (раздел 6) — документ подтверждён «готов к разработке». Блок 2 → 5/5.
- product-lead принял решение по блоку 3: настоящая Figma недоступна агентам (руки нужны человеку), блок переименован в «Дизайн — система и макеты», задачи закрываются HTML/CSS-прототипами вместо файлов Figma.
- Собрали и опубликовали первый артефакт блока 3 — «Кольца и Дни»: дизайн-система (цвета/типографика/сетка/радиусы) + живые компоненты (кольца КБЖУ тренировочный/день отдыха, карточка блюда с confidence, спортпит-грид, слайдер нормы белка) в тёмной и светлой теме — токены взяты один в один из требования к дизайну.md §2–3, не выдуманы заново.
- Задачник обновлён: 11/75 (15%). Отчёт записан в [[отчёты]] (product-lead, сводка за ui-ux-designer).
**Дальше:** Блок 3 продолжается — макеты 9 экранов, онбординг-квиз, лендинг, кликабельный прототип (соединяющий экраны переходами). После блока 3 — backend (блок 4) и frontend-каркас (блок 7) можно вести параллельно, оба ждут только готовую дизайн-систему, которая уже есть.
**Заметки:** Каждый шаг такого масштаба продолжаю сопровождать: отчёт в [[отчёты]], галочка в [[задачник]], коммит. Полная разработка (75 пунктов) — это много волн, не один присест; буду продолжать по одной-двум задачам блока за раз, а не пытаться сделать всё сразу низким качеством.

---

## 2026-08-22 (4)
**Сделано:**
- Объяснили механику разрыва hard link: Edit-инструмент не дописывает файл на месте, а атомарно подменяет его новым (защита от порчи при сбое) — путь в `.claude\agents\` начинает указывать на новый файл, путь в `агенты\...` — на старый. Так расходится содержимое.
- Пробовали настоящий фикс — заменить hard link на symlink (он переживает подмену файла, т.к. хранит путь, а не физический файл). **Не получилось:** `New-Item -ItemType SymbolicLink` требует прав администратора или включённого Developer Mode — ни того ни другого на этой машине нет. По пути ненадолго остались без копий в `агенты\...` (Remove-Item отработал, New-Item — нет) — сразу восстановили через hard link, проверили по хешу, ничего не потеряно (плюс всё это уже было в git).
- Спросили пользователя explicit: автоматизировать пересборку hook'ом, включить Developer Mode, или чинить вручную как раньше. **Решение: чинить вручную** — пересобирать hard link PowerShell'ом (`Remove-Item` + `New-Item -ItemType HardLink`) после каждой правки любого из 11 файлов агентов через Edit-инструмент.
**Дальше:** Ничего не меняли по существу (`Agent`-инструмент и отчёты.md из прошлой записи остались как есть) — эта запись фиксирует только диагностику и решение не менять механизм синхронизации.
**Заметки:** **Важно на будущее:** если снова нужно будет чинить hard link — НЕ пробовать `-ItemType SymbolicLink` на этой машине без прав/Developer Mode, сразу использовать `-ItemType HardLink`. Скрипт-заготовку в отдельном `.ps1`-файле лучше не использовать для кириллических путей через `powershell -File` — Windows PowerShell 5.1 читает `.ps1` без BOM в системной кодировке (не UTF-8), кириллица превращается в мусор («УРОК» → «РЈР РћРљ»); либо сохранять `.ps1` с BOM, либо (проще) выполнять команды напрямую через инструмент PowerShell, а не через файл.

---

## 2026-08-22 (3)
**Сделано:**
- Уточнили: субагенты (`Agent` в `tools:`) — это метаданные во frontmatter, не заметка и не связь `[[wikilink]]`, поэтому на графе Obsidian их в принципе не видно (ни `Plan`/`Explore` — это встроенные типы Claude Code без файла в хранилище, ни право звать `security-reviewer` — оно не оформлено как wikilink). Договорились не выдумывать искусственный узел ради графа.
- Завели новый файл [[отчёты]] — короткий отчёт по каждой законченной задаче (агент/дата/что сделано/что дальше), отдельно от [[журнал]] (мой дневник сессий) и [[задачник]] (чек-лист статусов). Пишут «главные» агенты — [[product-lead]], [[backend-developer]], [[frontend-developer]], [[qa-devops]] — своей задачей или после субагента; остальные 7 пишут сами, если ведут задачу напрямую без делегирования.
- Прописали это правило в [[CLAUDE]] (новый раздел «Отчёты агентов»), в [[команда]] и в самих файлах 4 «главных» агентов (раздел/строка «Отчёт» — допиши запись в отчёты.md после завершения задачи).
- Пересобрали hard link `.claude\agents\` ↔ `агенты\...` для этих 4 файлов (снова разошлись после Edit) — сверено по хешу.
**Дальше:** Пока `отчёты.md` пуст — первая запись появится, когда кто-то из 4 «главных» агентов реально закончит задачу блока 3+ (по задачнику). Отдельно всё ещё актуально: проверка [[требования к дизайну]] дизайнером перед Figma.
**Заметки:** Если после следующей правки любого из 4 файлов агент вдруг «не видит» отчёты.md/Agent-инструмент — сначала проверь хеши `.claude/agents/<файл>.md` vs `агенты/.../<файл>.md` (см. запись выше и за 21 августа про hard link).

---

## 2026-08-22 (2)
**Сделано:**
- По скриншоту графа выяснили: перенос файлов в `архив\` физически сработал (виден в проводнике Obsidian), но Graph view без фильтра всё равно рисует заметки из архива, пока на них есть `[[wikilink]]` — это ожидаемо, не баг.
- Закрыли это на двух уровнях: `.obsidian/app.json` → `userIgnoreFilters: ["архив/"]` (Excluded Files — прячет архив из графа/поиска/quick switcher глобально) + `.obsidian/graph.json` → `search: -path:"архив/"` как дублирующая защита. **Важно:** после этой правки нужен перезапуск Obsidian (просто Reload вкладки графа может не подхватить Excluded Files).
- Ревизия команды: выдали инструмент `Agent` (возможность звать субагентов) четырём агентам, обоснованно под каждого — [[product-lead]] (реально делегирует 10 агентам, а не только советует), [[backend-developer]] и [[frontend-developer]] (вызывают `Plan` перед крупным архитектурным блоком/несколькими экранами разом), [[qa-devops]] (обязательный гейт `security-reviewer` перед релизом + `Explore` для проверки покрытия тестами). Остальным 7 агентам `Agent` осознанно не давали — не нужно под их задачи.
- Добавили раздел «Субагенты — кто может делегировать» в [[команда]] с таблицей и обоснованием — чтобы это было видно прямо в Obsidian, а не только в конфигах.
- Правка агентов через Edit снова порвала hard link между `.claude\agents\` и `агенты\...` (как и 21 августа) — пересобрали PowerShell'ом (`Remove-Item` + `New-Item -ItemType HardLink`), сверили по хешу — совпадают.
- Обновили [[CLAUDE]]: с этой сессии журнал и коммит делаются в конце сессии (и после значимых правок) автоматически, без вопроса «записать ли» — пользователь так попросил явно.
**Дальше:** По задачнику — всё ещё актуален пункт: проверка [[требования к дизайну]] дизайнером перед переносом в Figma. Отдельно: если появится реальный код (блоки 4–9), стоит пересмотреть, не нужен ли `Agent`/`Explore` ещё кому-то (например market-analyst для параллельного исследования конкурентов).
**Заметки:** Если Graph view в Obsidian снова показывает архивные заметки — сначала проверь, не сброшен ли `userIgnoreFilters` в `.obsidian/app.json` (мог быть очищен вручную через UI) и перезапущено ли приложение после правки. Если `tools:` у агента вдруг не работает как ожидается (например Agent-инструмент не виден) — проверь, не разошлись ли хеши `.claude/agents/<файл>.md` и `агенты/.../<файл>.md` (см. выше про hard link).

---

## 2026-08-22
**Сделано:**
- Разобрали список подключённых Claude Code Skills — 1 глобальный (`find-skills`, `~/.claude/skills`) и 21 локальный, установленный именно в этот проект (`.claude/skills`, зафиксированы в `skills-lock.json`) — сводка уже была в [[карта скилов]].
- Проверили агентов на предмет «deep research»: отдельного режима/модели ни у кого не назначено, реальный веб-поиск (WebSearch/WebFetch) есть только у market-analyst, growth-marketer и частично у brand-designer.
- По скриншоту графа Obsidian нашли путаницу: черновики старого прогона «агент 1–5 — ...» в корне визуально путались с настоящими 11 агентами команды.
- Навели порядок: создали `архив\` и перенесли туда через `git mv` (история сохранена) — 5 черновиков «агент 1–5 — ...» (устарели, заменены финальными [[идея]]/[[требования к дизайну]], но остаются как источник — ссылки на них из ui-ux-designer/brand-designer/market-analyst/задачник не сломаны, Obsidian резолвит `[[wikilink]]` по имени файла) и 4 файла-мусора без отношения к проекту (`Claude` пустой, `date_of_birth.txt`, `test.txt`, `исследование.html` — старый HTML-прототип, не связан с исследование.md).
- Настраивали фильтр Graph view (`.obsidian/graph.json`) на показ только `команда` + `карта скилов` + `агенты\` — пользователь потом сам очистил фильтр вручную.
- Закоммитили перемещение в архив и эту запись журнала.
**Дальше:** По задачнику — всё ещё актуален пункт: проверка [[требования к дизайну]] дизайнером перед переносом в Figma.
**Заметки:** Договорились: журнал ≠ git-коммит — разные действия, коммичу только по прямой просьбе.

---

## 2026-08-21
**Сделано:**
- Заведён этот журнал и [[CLAUDE]] — чтобы сессии не начинались с чистого листа. Claude теперь читает журнал.md и [[задачник]] автоматически в начале сессии.
- Найден и запущен Obsidian на компьютере, открыт как vault на папке `УРОК 1`.
- Подключён MCP-сервер `obsidian` (`@modelcontextprotocol/server-filesystem`, scope local) — статус Connected; инструменты появятся с новой сессии.
- Разобрались, как передавать скриншоты в чат на Windows (Ctrl+V не работает в терминале): `Win+Shift+S` → «готово» → достаю картинку из буфера через PowerShell (`System.Windows.Forms.Clipboard`) и читаю.
- Продиагностировали, почему граф в Obsidian не похож на граф из обучающего видео — рабочие документы (идея, техзадание, задачник и т.д.) были упомянуты простым текстом в обратных кавычках, а не `[[wikilink]]`, поэтому висели в графе изолированными точками.
- Связали все такие упоминания в `[[wikilinks]]` по 22 файлам (документы + все 11 агентов) — граф теперь один связный кластер.
- Побочно найдено и исправлено: файлы в `.claude\agents\` были hard-link копиями файлов из `агенты\`, но правки через Edit-инструмент рвут hard link — пересобрали связи заново. **Важно:** это будет рваться при каждой следующей правке агента, чинить придётся вручную (PowerShell `Remove-Item` + `New-Item -ItemType HardLink`).
- В `команда.md` добавили две новые колонки в таблицы состава команды: «Кратко» (2 слова на агента, например `ui-ux-designer` → «рисует макет») и «Кто это (простыми словами)» — понятное объяснение роли для новичка.
**Дальше:** По задачнику (9/75, 12%) — ближайший незакрытый пункт: проверка текстового ТЗ ([[требования к дизайну]]) дизайнером (`ui-ux-designer`) перед переносом в Figma. После этого — блок 3 (дизайн в Figma). Отдельно: проверить MCP-инструменты obsidian после следующего перезапуска сессии (отложили в этой сессии).
**Заметки:** Пользователь пишет записи сам в конце сессии (или диктует — я оформляю и дописываю). Без хуков/автоматики, по договорённости. Решили не переименовывать файлы агентов ради подписи прямо на графе (риск порвать ссылки) — короткая метка осталась только в таблице `команда.md`.
