# Отчёты агентов

*Короткий отчёт сразу после того, как агент закончил задачу — сам или получив результат от делегированного субагента. Пишут «главные» агенты — [[product-lead]], [[backend-developer]], [[frontend-developer]], [[qa-devops]] (у них есть право делегировать через `Agent`, см. [[команда]], раздел «Субагенты»). Остальные агенты не дублируют сюда каждый чих — если задачу вёл один из специалистов напрямую без делегирования, отчёт даёт тот, кто её вёл.*

*Не путать с [[журнал]] (хронологический дневник сессий целиком — что происходило и почему, веду я) и [[задачник]] (чек-лист ✅/❌ по блокам — текущий срез статуса). Здесь — что конкретно сделал агент по конкретной задаче. Новая запись — сверху.*

**Шаблон записи:**
```
## ГГГГ-ММ-ДД · Агент
**Задача:** ...
**Сделано:** ...
**Файлы/результат:** ...
**Что дальше / нужно от фаундера:** ...
```

---

## 2026-08-24 · backend-developer
**Задача:** Фаундер учится по видеокурсу (сейчас подготовительный период перед стартом основной учёбы) и хочет заранее разобраться в механике подключения ИИ через OpenRouter — это же понадобится для будущего Telegram-бота на курсе. Заодно разблокировать тестирование распознавания фото в блоке 11 (не было реального ключа ни для Anthropic, ни для чего-либо ещё).
**Сделано:**
- Добавлен `openrouter-vision.ts` — распознавание блюда по фото через OpenRouter (openrouter.ai): единый ключ, OpenAI-совместимый формат запроса (`POST /chat/completions`, `Authorization: Bearer`, `image_url` с data-URI). В отличие от `claude-vision.ts` не полагается на forced tool_choice — просит JSON текстом и достаёт его сам, так как модель под капотом `openrouter/free` заранее не известна.
- `RecognizedDish` вынесен в общий `meal-recognition.types.ts`, добавлен `ai-vision.ts` — фасад, переключающий провайдера по `AI_PROVIDER` (`openrouter` по умолчанию, `anthropic` — прежний путь). `api/routes/meals.ts` теперь импортирует только фасад.
- `env.ts`/`.env.example` дополнены: `AI_PROVIDER`, `OPENROUTER_API_KEY`, `OPENROUTER_MODEL` (по умолчанию `openrouter/free` — бесплатный роутер OpenRouter, не требует привязки карты, годится для проверки механики).
- Реально проверено: `tsc --noEmit` — 0 ошибок. **Не проверено живым запросом** — у фаундера пока нет `OPENROUTER_API_KEY`, инструкция как получить дана в чате/[[журнал]].
**Файлы/результат:** `backend/src/integrations/{openrouter-vision.ts, ai-vision.ts, meal-recognition.types.ts}`, правки в `claude-vision.ts`, `env.ts`, `.env.example`, `api/routes/meals.ts`, `integrations/README.md`.
**Что дальше / нужно от фаундера:** Зарегистрироваться на openrouter.ai, создать ключ (Dashboard → Keys), вставить в `backend/.env` как `OPENROUTER_API_KEY`. После этого — живой смок-тест распознавания фото, и можно продолжать блок 11 (юнит-тесты бизнес-логики уже есть с блока 6, но точность распознавания фото/нагрузочное/кроссбраузерное тестирование ещё не делались).

---

## 2026-08-24 · backend-developer (продолжение — живой тест)
**Задача:** Фаундер получил `OPENROUTER_API_KEY`, довели механику до реальной проверки: пошагово (терминал → скрипт), потом сам процесс запуска взял на себя я (тот же терминал, что и раньше в сессии) — фаундер прислал фото, я запустил.
**Сделано:**
- Написаны и реально выполнены `scripts/test-openrouter.ts` (сырой запрос к OpenRouter) и `scripts/test-recognize-meal.ts` (сама функция `recognizeMealPhoto` из `openrouter-vision.ts`).
- Найден и исправлен реальный баг: заголовок `X-Title` с кириллицей ломал `fetch()` (`TypeError: ... ByteString`) — HTTP-заголовки должны быть ASCII. Поймано именно живым тестом, не статическим анализом.
- Ключевая находка по механике: `OPENROUTER_MODEL="openrouter/free"` — рандомный роутер, не фиксированная модель. Три живых прогона на одном и том же фото дали три разных исхода: 3.8с/идеальный JSON, >60с без ответа (думающая модель), 52.7с/синтаксически битый JSON (даже с моделью, закреплённой явно). Наш fallback (`null` → ручной ввод, НФТ 5.1) сработал корректно во всех "плохих" случаях — это не баг, а рассчитанное поведение.
- Фаундеру предложен выбор (платная модель / бесплатная с оговорками / вернуться к Anthropic) — выбрано остаться на бесплатной осознанно, с пониманием ограничений.
**Файлы/результат:** `backend/scripts/{test-openrouter.ts, test-recognize-meal.ts, diag-openrouter-timing.ts}`, обновлены `openrouter-vision.ts` (фикс заголовка), `integrations/README.md`, [[задачник]].
**Что дальше / нужно от фаундера:** Блок 11 (точность распознавания на наборе реальных блюд) технически можно продолжать и на бесплатном тире, но результат будет зашумлён случайностью модели — когда/если решите потратить пару центов на конкретную платную модель, просто смените `OPENROUTER_MODEL` в `.env`, код менять не придётся.

---

## 2026-08-24 · qa-devops
**Задача:** Блок 11 — тестирование и качество, продолжение сессии после механики OpenRouter.
**Сделано:**
- Перепрогнаны 29 юнит-тестов бизнес-логики (блок 6) — все зелёные, ничего не отвалилось.
- Добавлен Playwright (`frontend/e2e/smoke.spec.ts`, `playwright.config.ts`) — три профиля: desktop-chromium, mobile-chromium (Pixel 7), mobile-chromium (вьюпорт iPhone 14, т.к. настоящий WebKit не скачался — сетевые ограничения среды). Тесты гоняются против **живого прода** (kbzhu-frontend.vercel.app), не локального dev-сервера.
- Покрытие: контент лендинга, отсутствие горизонтального скролла на мобильных, пропуск шага приветствия при `entry=landing` (§5.1), и полный технический прогон онбординг-квиза до `/main` через настоящую регистрацию (`/auth/register` → `/targets/today`) в демо-режиме.
- Найден и **исправлен вживую** реальный баг: `.heroGlow` (декоративный блик под hero, `max-width:160vw`) не был обрезан секцией `.hero` → раздувал ширину документа на мобильных вьюпортах на ~120px, из-за чего весь лендинг скроллился вбок и съезжал fixed-переключатель темы. Правка — `overflow-x: hidden` на `.hero` (`Landing.module.css`). Собрано (`npm run build` чисто), задеплоено на прод (`vercel --prod`, с подтверждением фаундера — деплой на прод классификатор блокирует без явного «да»), тесты перепрогнаны на исправленной версии — все 12 зелёные.
**Файлы/результат:** `frontend/{playwright.config.ts, e2e/smoke.spec.ts, .gitignore}`, `frontend/src/components/landing/Landing.module.css` (фикс), [[задачник]] блок 11 → 2/5, общий прогресс 48/75 (64%).
**Что дальше / нужно от фаундера:** Firefox/Safari — стоит разово проверить лендинг вручную с машины, где сеть не ограничена (эта рабочая среда не смогла скачать движки WebKit/Firefox). Для пункта «точность распознавания фото» нужно ещё несколько реальных фото блюд. Нагрузочное тестирование ещё не начато.

---

## 2026-08-24 · qa-devops (реальный баг на проде)
**Задача:** Фаундер сам протестировал загрузку фото в реальном приложении (kbzhu-frontend.vercel.app) и получил «ошибку, не распознаёт» — разобраться и починить по-настоящему, не гадая.
**Сделано:**
- Диагностика через `railway logs`: оба запроса `POST /meals/photo` вернули `202` (штатный "черновик без распознавания", не сбой) за 28–48мс — подозрительно быстро для настоящего вызова ИИ.
- Подтверждено без раскрытия секрета (`railway run -- node -e "!!process.env..."`): на Railway **не было вообще ни одного ключа** (`OPENROUTER_API_KEY`, `ANTHROPIC_API_KEY`) — сегодняшний ключ сохранялся только в локальный `backend/.env`, на прод не попадал.
- С явного подтверждения фаундера (permission-классификатор блокирует такие действия по умолчанию, и правильно): добавлена переменная `OPENROUTER_API_KEY`+`AI_PROVIDER`+`OPENROUTER_MODEL` на Railway, сервис перезапущен (`railway redeploy`).
- Повторный тест — всё ещё не работало (202, но уже 8–9с, а не мгновенно). Докопался до второй, более глубокой причины: **на Railway всё ещё был задеплоен старый код бэкенда** — `railway up` последний раз запускался ещё в блоке 10, до сегодняшней OpenRouter-интеграции; CI/CD нет (блок 10, п.2 ❌), деплой ручной, поэтому все правки этой сессии просто не доехали до прода. С подтверждения фаундера — `railway up --detach`, дождался нового контейнера в логах.
- Финальная сквозная проверка **на реальном проде, реальным фото фаундера** (curl: регистрация → `/meals/photo`): пайплайн действительно работает — при удачном раскладе модель верно распознала лосось (принят за «мясо на гриле»), яйцо, салат, помидоры, кукурузу. Но при нескольких повторах бо́льшая часть попыток всё равно возвращала пустой черновик — то же самое известное свойство бесплатного роутера (см. отчёт от того же дня про механику OpenRouter), теперь подтверждённое и на проде, не только в тестовых скриптах.
- Добавлено логирование причины отказа (`console.warn` в `openrouter-vision.ts` — статус ответа/пустой content/невалидный JSON/таймаут) — раньше приходилось гадать и городить diag-скрипты, теперь видно сразу в `railway logs`.
- Фаундеру задан вопрос ещё раз (после личного столкновения с проблемой в реальном приложении) — решение подтверждено: остаёмся на бесплатной модели осознанно.
**Файлы/результат:** `backend/src/integrations/openrouter-vision.ts` (логирование), переменные окружения + код на Railway (прод), [[задачник]] пункт «точность распознавания» → ⚠.
**Что дальше / нужно от фаундера:** Ничего срочного — поведение теперь ожидаемое и объяснимое, не баг. **Важный урок процесса**: пока нет CI/CD (блок 10), любая правка бэкенда требует отдельного ручного `railway up` — иначе прод молча остаётся на старом коде, как сегодня.

---

## 2026-08-22 · product-lead (сводка за ui-ux-designer)
**Задача:** Закрыть последний пункт блока 2 (проверка ТЗ на дизайн перед Figma) и начать блок 3.
**Сделано:**
- ui-ux-designer сверил [[требования к дизайну]] с [[техзадание]] по таблице трассируемости (раздел 1) и разделом самопроверки (раздел 6) — документ подтверждён как «готов к разработке», расхождений не найдено. Блок 2 закрыт (5/5).
- Решение продакт-лида: настоящая Figma требует рук человека (см. [[команда]]) — блок 3 переименован из «Дизайн в Figma» в «Дизайн — система и макеты», задачи закрываются кликабельными HTML/CSS-прототипами вместо файлов Figma. Название блока изменил, содержание задач не менял.
- Первый пункт блока 3 сделан: дизайн-система как рабочий прототип (цвета, типографика, сетка, радиусы, кольца КБЖУ, карточка блюда, спортпит-грид, слайдер белка) — в тёмной и светлой теме, токены взяты один в один из [[требования к дизайну]] §2–3.
**Файлы/результат:** Артефакт «Кольца и Дни» — https://claude.ai/code/artifact/0bad7d6f-350d-48e7-bf5b-210368d0f2ef · обновлён [[задачник]] (11/75, 15%).
**Что дальше / нужно от фаундера:** Дальше по блоку 3 — макеты 9 экранов, онбординг-квиз, лендинг, кликабельный прототип. От фаундера ничего не нужно — можно просто посмотреть артефакт и сказать, если что-то в дизайне не нравится, до того как это разойдётся по 9 экранам.

---

## 2026-08-22 · ui-ux-designer
**Задача:** Блок 3 — макеты 9 экранов MVP, онбординг-квиз, кликабельный прототип.
**Сделано:** Собрал всё в одном интерактивном артефакте вместо статичных картинок — реальная навигация, не имитация: онбординг (9 содержательных шагов + опциональные шаг 0 и время тренировки, §5.1) с настоящим расчётом нормы по формуле Миффлина-Сан Жеора (не заглушка), дальше все 9 экранов продукта (§3.2) с рабочими переходами — главный экран с переключателем тип дня (кольца пересчитываются и перекрашиваются), фото → скелетон-загрузка → карточка блюда, штрихкод → bottom sheet, спортпит (креатин-стрик + категории → список → добавить), дневник (офлайн-баннer, группировка по приёму пищи), вес (график + карточка автокоррекции), отчёты (столбцы по типу дня), настройки (тема/цель/тренировочные дни/дисклеймер). Тёмная/светлая тема — общий переключатель, состояние живёт в localStorage.
**Файлы/результат:** Артефакт «Кольца и Дни — Прототип» — https://claude.ai/code/artifact/93e4c4f6-80b3-4c37-9a61-e3b6b58436bd · [[задачник]] обновлён (14/75, 19%), блок 3 — 4/5.
**Что дальше / нужно от фаундера:** Остался последний пункт блока 3 — макеты лендинга (отдельная страница, другая аудитория, буду делать отдельным артефактом). После блока 3 — backend (блок 4) и frontend-каркас (блок 7) можно вести параллельно, дизайн-система для обоих уже готова. Founder: посмотри прототип вживую — если поведение экрана (не только внешний вид) не совпадает с ожиданиями, лучше поймать это сейчас, а не после переноса в Next.js.

---

## 2026-08-22 · ui-ux-designer
**Задача:** Закрыть последний пункт блока 3 — макеты лендинга (все секции по §4 требования к дизайну.md).
**Сделано:**
- Отдельный артефакт-лендинг: hero (два мокапа — тренировочный день/день отдыха бок о бок), таблица «обычный счётчик vs наш продукт», 3 ключевые фичи по фиксированному порядку (циклирование углеводов → спортпит → автокоррекция), демо-лента «день из жизни», блок доверия (ISSN-ссылка + прозрачная формула, без придуманных цифр отзывов), pricing (Базовый/Pro, переключатель помесячно/ежегодно, сравнительная таблица), финальный CTA, FAQ на 8 вопросов, футер с дисклеймером. Та же дизайн-система, что и в приложении — токены не придуманы заново.
- Заодно (не отдельными задачами задачника, но по ходу тестирования прототипа приложения пользователем) поправил в прототипе: рабочую кнопку «Назад» на шагах онбординга, стимпанк-пикер времени тренировки (точечный акцент по просьбе), скролл-колёсики для возраста/роста/веса вместо текстовых полей, карточку профиля в «Отчётах» с кнопкой «Изменить данные», убрал нерабочую/перекрывающую текст кнопку быстрого веса и заменил её на конкретную строку-предложение по корректировке нормы.
**Файлы/результат:** Артефакт-лендинг — https://claude.ai/code/artifact/72b91469-c074-4e36-b61d-c4397848e18d · [[задачник]]: блок 3 закрыт (5/5) ✅, общий прогресс 15/75 (20%).
**Что дальше / нужно от фаундера:** Блок 3 полностью закрыт. Дальше — блок 4 (backend-developer: инфраструктура и БД) и блок 7 (frontend-developer: каркас Next.js) можно вести параллельно, дизайн-система для обоих готова в двух артефактах (прототип приложения + лендинг). Founder: посмотри лендинг так же вживую, как и прототип приложения — присылай, что не так.

---

## 2026-08-22 · backend-developer
**Задача:** Блок 4 — инфраструктура и БД (первый реальный код проекта, не документация/артефакт).
**Сделано:** Каркас в `backend/` — Node.js + TypeScript + Fastify. Схема БД на 6 таблиц (users/meals/meal_items/supplement_logs/daily_targets/weight_logs) в `prisma/schema.prisma`, поля напрямую из идея.md/техзадание.md (в т.ч. ограничение белка выше 3,0 г/кг — `proteinOverrideConfirmedAt`). Валидация переменных окружения при старте. Аутентификация (регистрация/логин/JWT) с полями онбординг-квиза. Роуты `/me`, `/meals` (ручной ввод + приём фото под будущее распознавание), `/weight`, `/supplements`. Хранилище фото S3-совместимое (загрузка/подписанная ссылка/удаление/поиск просроченных по политике хранения).
**Не имитация — реально проверено:** `npm install` + `prisma generate` прошли, `tsc --noEmit` — 0 ошибок, поднял dev-сервер и смок-тестом curl'ом проверил: `/health` ок, `/health/db` корректно отличает «процесс жив» от «нет связи с БД», `/auth/register` возвращает понятные ошибки валидации, `/me` без токена — 401. Тестовый `.env` не оставлен в репозитории.
**Файлы/результат:** `backend/` (21 файл, см. README.md там же) · [[задачник]]: блок 4 закрыт (5/5) ✅, общий прогресс 20/75 (27%).
**Что дальше / нужно от фаундера:** Блок 5 (интеграции: Claude API распознавание, Open Food Facts, USDA) и блок 6 (бизнес-логика: расчёт нормы, циклирование, автокоррекция) — логичное продолжение backend, либо блок 7 (frontend-developer, каркас Next.js) параллельно. От фаундера: нужен реальный ключ Claude API и решение по S3-провайдеру (AWS vs Cloudflare R2) до блока 5 — это разъясняется, не блокирует прогресс сейчас.

---

## 2026-08-22 · backend-developer
**Задача:** Блоки 5 (интеграции) и 6 (бизнес-логика) — по просьбе фаундера "делай всё", продолжение backend без остановки.
**Сделано:**
- Блок 5: Claude API (структурированный tool-use вывод, распознавание блюда с confidence), Open Food Facts (штрихкоды — сеть реально проверена), USDA FoodData Central (сверка калорийности), общий http-клиент с таймаутом/повтором и graceful fallback (null, не исключение — везде ведёт к ручному вводу, а не к ошибке 500), in-memory TTL-кэш нутриентов (24ч).
- Блок 6: расчёт нормы (Миффлин-Сан Жеора), циклирование углеводов по типу дня (белок постоянен, углеводы/жиры меняются), автокоррекция по тренду веса раз в 7–14 дней, тайминг напоминаний вокруг тренировки + вечерняя сводка по белку, жёсткий потолок белка как второй уровень защиты поверх UX-гейта блока 4.
- Всё это подключено к реальным роутам: `/meals/photo` (распознавание), `/meals/barcode` (новый роут), `/targets/today` + ручной тоггл типа дня + "ближайшее" + вечерняя сводка (новый файл `targets.ts`), `/weight/suggestion`. При регистрации норма на сегодня считается сразу, не только при первом заходе на главный экран.
- Попутно нашёл и исправил реальный баг типизации: `sex` в схеме БД был обычной строкой, а не Prisma-enum — из-за этого TypeScript не мог гарантировать "m"/"f" при передаче в расчёт нормы. Сделал полноценным enum, как Goal/Theme.
**Проверено реально:** `tsc --noEmit` — 0 ошибок, 29 юнит-тестов бизнес-логики (vitest) — все зелёные, сервер поднят и curl'ом проверены новые роуты (401 без токена на всех защищённых, health по-прежнему ок). Open Food Facts — реальный сетевой запрос прошёл. Claude API и USDA — **не протестированы вживую**, нет реальных ключей; код по документации SDK, типизация проходит, но живого вызова не было.
**Файлы/результат:** `backend/src/integrations/` (+4 файла), `backend/src/services/` (+4 файла + 4 файла тестов), новый роут `targets.ts`, правки в `meals.ts`/`weight.ts`/`auth.ts`. [[задачник]]: блоки 5 и 6 закрыты (5/5 каждый), общий прогресс 30/75 (40%).
**Что дальше / нужно от фаундера:** Блок 7 (frontend-developer, каркас Next.js) — следующий логичный шаг, дизайн-система и API уже готовы. Для полной проверки блока 5 понадобятся от фаундера реальные `ANTHROPIC_API_KEY` и `USDA_FDC_API_KEY` — без них код рабочий, но не проверен живым вызовом.

---

## 2026-08-22 · frontend-developer
**Задача:** Блок 7 — инициализация Next.js и дизайн-система в коде.
**Сделано:** `frontend/` — Next.js (App Router, TypeScript), без Tailwind — токены (цвета/типографика/сетка/радиусы) перенесены напрямую из требования к дизайну.md §2 в globals.css, тот же трёхсторонний theming (система/явный выбор), что в артефактах-прототипах. Компонентная библиотека: Button, Card, MacroRing (один узел для большого и малых колец), DayPill, ConfidenceBadge, TabBar, ScreenHeader. Роутинг всех 9 экранов + онбординга, 5 табов под общим таб-баром, фото/штрихкод/спортпит — полноэкранные роуты с «Назад». `/main` собран по-настоящему (не заглушка) — демонстрирует, что компоненты складываются в реальный экран с работающим тогглом типа дня.
**Проверено реально:** `npm run build` — 0 ошибок TypeScript, все 10 маршрутов сгенерированы; `eslint .` — чисто; dev-сервер поднят, curl'ом подтверждено, что `/main` рендерит реальный текст компонентов, редирект `/` → `/onboarding` работает.
**Файлы/результат:** `frontend/` (проект целиком) — см. README.md там же. [[задачник]]: блок 7 закрыт (5/5), общий прогресс 35/75 (47%).
**Что дальше / нужно от фаундера:** Блок 8 (экраны продукта — наполнить оставшиеся 8 заглушек реальным содержанием и подключить к backend API) или блок 9 (лендинг в коде, дизайн уже готов артефактом). От фаундера ничего не нужно.

---

## 2026-08-22 · frontend-developer
**Задача:** Блок 8 — наполнить все 9 экранов реальным содержанием и подключить к backend API.
**Сделано:** Тонкий API-клиент (JWT в localStorage) и auth-контекст. Онбординг — полный квиз с реальной регистрацией и расчётом нормы. Главный экран — реальный тоггл типа дня и блок «Ближайшее». Фото — загрузка + распознавание с честным fallback на ручной ввод. Штрихкод — ручной ввод номера (реальный сканер камерой не входил в этот блок). Спортпит — категории → порции (справочника ≥50 позиций с поиском в блоках 5/6 не было, статичный список категорий, отмечено как открытая задача). Дневник — автогруппировка по времени суток, удаление. Вес — график тренда, карточка-предложение автокоррекции с реально работающими «Принять»/«Отклонить». Отчёты — недельный график по типу дня, среднее по белку. Настройки — профиль, тренировочные дни, тонкая настройка белка с подтверждающим диалогом выше 3,0 г/кг, тема, удаление аккаунта.
**Побочно поправил backend:** добавил `POST /targets/today/apply-adjustment` (раньше автокоррекция только считалась, не применялась — реальный пробел, найденный при подключении экрана «Вес») и `GET /targets/history` (для недельного графика отчётов).
**Проверено реально:** `npm run build` 0 ошибок TS, `eslint .` чисто (включая новые строгие правила `react-hooks/purity`/`react-hooks/set-state-in-effect` этой версии React — два реальных нарушения найдены и исправлены, не проигнорированы), backend `tsc --noEmit` по-прежнему 0 ошибок после добавления роутов, dev-сервер фронтенда поднят, curl'ом проверены все 10 маршрутов (200, без 500), онбординг рендерит реальный текст.
**Не проверено:** полный цикл через реальный backend с живой БД — в песочнице нет Postgres, только структурная проверка (build/lint) с обеих сторон.
**Файлы/результат:** `frontend/src/lib/` (api, types, auth-context), все 9 экранов в `frontend/src/app/`, 2 новых роута в `backend/src/api/routes/targets.ts`. [[задачник]]: блок 8 закрыт (5/5), общий прогресс 40/75 (53%) — больше половины.
**Что дальше / нужно от фаундера:** Блок 9 (лендинг в коде — дизайн и тексты уже готовы артефактом) — логичное продолжение frontend. Открытый вопрос на будущее (не блокирует): нужен ли реальный сканер штрихкода камерой и полноценный справочник спортпита — оба сейчас упрощены сознательно.

---

## 2026-08-22 · frontend-developer
**Задача:** Блок 9 — лендинг в коде (требования к дизайну.md §4), продолжение того же артефакта, что уже проверял фаундер.
**Сделано:** Маршрут `/` — hero (2 мокапа тип дня), сравнение «обычный счётчик vs наш продукт», 3 фичи в фиксированном порядке, демо-лента «день из жизни», блок доверия (ISSN + прозрачная формула — без придуманных цифр отзывов, честно по спецификации), pricing на 2 тарифа с переключателем помесячно/ежегодно (Pro+ не показан — нет наполнения для MVP), FAQ на 8 вопросов через `<details>` (без JS), футер с дисклеймером. Все на общих токенах продукта — не отдельная копия палитры. Переключатель темы — общий localStorage-ключ с продуктом.
**Важная деталь по спецификации:** CTA лендинга ведут в `/onboarding?entry=landing` — шаг 0 (приветствие) квиза при этом пропускается, ровно как требует §5.1 сноска * («лендинг уже выполнил его функцию»). Раньше (блок 8) онбординг всегда показывал шаг 0 — этот блок довёл его до полного соответствия документу.
**Проверено реально:** `npm run build` 0 ошибок TS (все 11 маршрутов), `eslint .` чисто — по дороге нашёл и по-настоящему исправил ещё одно нарушение `react-hooks/set-state-in-effect` (в ThemeSwitch). Dev-сервер поднят, curl'ом подтверждено: заголовок/мета-описание рендерятся, `/onboarding?entry=landing` реально стартует с шага «Твоя цель», а не с приветствия.
**Файлы/результат:** `frontend/src/app/page.tsx`, `frontend/src/components/landing/` (Landing.module.css, ThemeSwitch, PricingCards). [[задачник]]: блок 9 закрыт (5/5), общий прогресс 45/75 (60%).
**Что дальше / нужно от фаундера:** Backend/frontend по MVP (блоки 4-9) теперь полностью закрыты. Дальше — блок 10 (DevOps: хостинг, CI/CD, мониторинг) и блок 11 (тестирование сверх уже написанных 29 юнит-тестов бизнес-логики: кроссбраузерное, нагрузочное). Это уже требует реальных решений фаундера (хостинг-провайдер, домен) — не всё можно сделать в песочнице без него.

---

## 2026-08-22 · qa-devops
**Задача:** Блок 10, пункт 1 — реальный деплой frontend+backend для тестирования (по запросу фаундера — «хочу зайти по адресу и потыкать»).
**Сделано:** Установлен PostgreSQL локально (winget). Backend + Postgres задеплоены на Railway: создан проект, сгенерирована и применена первая Prisma-миграция (через локальный Postgres, т.к. напрямую до облачной БД с этой машины по прокси не достучаться — обходной путь сработал), настроены переменные окружения (DATABASE_URL как ссылка на сервис Postgres, JWT_SECRET сгенерирован), задеплоен код, поднят публичный домен. Frontend задеплоен на Vercel с `NEXT_PUBLIC_API_URL`, указывающим на Railway backend. Попутный фикс: `uploadMealPhoto` больше не падает с ошибкой, если S3 не настроен (ключей пока нет) — просто пропускает сохранение фото, остальной флоу (в т.ч. сама попытка распознавания) не блокируется.
**Важная находка, записана отдельно в [[хостинг]]:** из рабочей среды этой сессии не достучаться до доменов `*.up.railway.app`/`*.proxy.rlwy.net` (таймаут при этом на обычных сайтах и даже на `railway.com` — ограничение именно моей среды выполнения, не самого деплоя). Проверено и подтверждено фаундером напрямую из браузера — оба адреса реально отвечают.
**Файлы/результат:** `хостинг.md` (новый, зафиксировано решение — Railway/Vercel для теста, свой Timeweb-сервер для реального релиза). Живые адреса: https://kbzhu-frontend.vercel.app (frontend), https://kbzhu-api-production.up.railway.app (backend). [[задачник]]: блок 10 → 1/5, общий прогресс 46/75 (61%).
**Что дальше / нужно от фаундера:** Можно тестировать прямо сейчас по ссылке на frontend — полный флоу (регистрация/онбординг, дневник, вес, отчёты, настройки) должен работать; фото не будет по-настоящему распознаваться ИИ (нет ключа Claude API — фаундер осознанно выбрал тестировать без него). Остальной блок 10 (CI/CD, мониторинг, бэкапы БД, security-меры) и блок 11 (более широкое тестирование) — по-прежнему впереди.

---
